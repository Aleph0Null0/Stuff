#include <iostream>

int fibonacci(int n, int total=0) {
    int prev = n;
    if (total >= 4000000) {
        return value-n;
    if (n<1) {
        return 0;
    }
    else {
        total += n;
    }
        return fibonacci(n+prev,total);
    }
}
int main() {
    std::cout << fibonacci(3);
}